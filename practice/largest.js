const number1 = Number(prompt("Enter the first number: s "))
const number2 = Number(prompt("Enter the second number: s "))
const number3 = Number(prompt("Enter the third number: s "))

const max = Math.max(number1, number2, number3)

alert("The largest number is: s " + max)